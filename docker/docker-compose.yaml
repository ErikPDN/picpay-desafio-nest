services:
  postgres:
    image: postgres:16-alpine
    container_name: picpay-desafio-nest-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: picpay_desafio_nest
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
